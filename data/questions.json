[
{,
    "correct_answer": 1,
    "explanation": "The `print()` function is used to display output on the screen, which is also known as the console."
  },
  {
    "question": "Which symbol is used for a single-line comment in Python?",
    "options": [
      "//",
      "#",
      "--",
      "/* */"
    ],
    "correct_answer": 1,
    "explanation": "In Python, the `#` symbol is used to create a single-line comment. Any text following the `#` on that line is ignored by the program."
  },
  {
    "question": "What is the correct way to declare a variable in Python?",
    "options": [
      "variable name = value",
      "name = value",
      "set name = value",
      "declare name = value"
    ],
    "correct_answer": 1,
    "explanation": "In Python, you create a variable by simply writing its name followed by an equals sign (`=`) and the value you want to store."
  },
  {
    "question": "What is the output of the following code snippet?\n\n```python\nprint(2 + 3 * 5)\n```",
    "options": [
      "25",
      "17",
      "10",
      "Error"
    ],
    "correct_answer": 1,
    "explanation": "Python follows the order of operations. Multiplication (`*`) is performed before addition (`+`), so `3 * 5` is 15, and `2 + 15` is 17."
  },
  {
    "question": "What does the `len()` function do in Python?",
    "options":,
    "correct_answer": 1,
    "explanation": "The `len()` function returns the number of items in an object, such as the number of characters in a string or the number of items in a list."
  },
  {
    "question": "What is the correct way to define a list in Python?",
    "options": [
      "{1, 2, 3}",
      "",
      "(1, 2, 3)",
      "\"1, 2, 3\""
    ],
    "correct_answer": 1,
    "explanation": "Lists in Python are defined using square brackets ``. They are used to store multiple items in a single variable."
  },
  {
    "question": "Which of the following data types is mutable (can be changed) in Python?",
    "options": [
      "string",
      "tuple",
      "list",
      "int"
    ],
    "correct_answer": 2,
    "explanation": "A list is a mutable data type, which means you can add, remove, or change its elements after it has been created. Strings, tuples, and integers are immutable."
  },
  {
    "question": "What is the correct syntax for a `for` loop in Python?",
    "options": [
      "for i in range(10):",
      "for i = 0; i < 10; i++:",
      "foreach i in range(10):",
      "loop i in range(10):"
    ],
    "correct_answer": 0,
    "explanation": "The correct syntax for a `for` loop uses the `for` keyword, followed by a variable name, the `in` keyword, and an iterable like `range()`."
  },
  {
    "question": "Which keyword is used to define a function in Python?",
    "options": [
      "function",
      "def",
      "define",
      "create"
    ],
    "correct_answer": 1,
    "explanation": "The `def` keyword is used to define a function in Python, followed by the function name and parentheses."
  },
  {
    "question": "What is the correct way to access the value for the key 'age' from the dictionary `person = {'name': 'John', 'age': 30}`?",
    "options": [
      "person['age']",
      "person.age",
      "person.value('age')",
      "person.key"
    ],
    "correct_answer": 0,
    "explanation": "Dictionary values are accessed using square brackets `` with the key name inside, such as `person['age']`."
  },
  {
    "question": "What is the output of the following code snippet?\n\n```python\nx = 5\ny = 2\nprint(x ** y)\n```",
    "options": [
      "10",
      "25",
      "7",
      "52"
    ],
    "correct_answer": 1,
    "explanation": "The `**` operator in Python performs exponentiation. `x ** y` means 5 to the power of 2, which is `5 * 5`, or 25."
  },
  {
    "question": "What does the `strip()` method do on a string?",
    "options":,
    "correct_answer": 1,
    "explanation": "The `strip()` method is a useful string method that removes any whitespace characters (like spaces, tabs, and newlines) from the beginning and end of a string."
  },
  {
    "question": "What is the correct way to check if a key exists in a dictionary in Python?",
    "options": [
      "key in dictionary",
      "dictionary.contains(key)",
      "dictionary.exists(key)",
      "dictionary[key]"
    ],
    "correct_answer": 0,
    "explanation": "The `in` operator is the correct and most Pythonic way to check for a key's existence in a dictionary."
  },
  {
    "question": "Which keyword is used to exit from a loop prematurely?",
    "options": [
      "break",
      "stop",
      "exit",
      "end"
    ],
    "correct_answer": 0,
    "explanation": "The `break` keyword is used to terminate the execution of the nearest enclosing loop. It is useful for breaking out of a loop early based on a condition."
  },
  {
    "question": "What is the correct way to create a tuple in Python?",
    "options": [
      "{1, 2, 3}",
      "",
      "(1, 2, 3)",
      "tuple(1, 2, 3)"
    ],
    "correct_answer": 2,
    "explanation": "A tuple is an ordered, immutable collection of items defined using parentheses `()`."
  },
  {
    "question": "What is the output of the following code?\n\n```python\nmy_string = \"Hello World\"\nprint(my_string.lower())\n```",
    "options":,
    "correct_answer": 1,
    "explanation": "The `.lower()` method returns a new string with all of the characters converted to lowercase."
  }
]